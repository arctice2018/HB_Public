第 6 课
========================================================

### 欢迎语
注释：
```{r}
library(ggplot2)
library(tidyr)
library(dplyr)
library(reshape2)
library(gridExtra)

```
```{r}
dm <- read.csv('diamondsbig.csv')
```

***

### 散点图回顾
#### price变量中存在空值，如果不设置na.rm为True,那么会报错，
```{r Scatterplot Review}
ggplot(data = dm,aes(x = carat,y = price)) +
  geom_point(color= I('black'),fill=I('blue'),alpha =.5,shape =21)+
  scale_x_continuous(limits = c(0,quantile(dm$carat,0.99)))+
  scale_y_continuous(limits = c(0,quantile(dm$price,0.99,na.rm = TRUE)))
```

***

### 价格与克拉的关系
回复：非线性关系
```{r}
ggplot(data = dm,aes(x = carat,y = price)) +
  geom_point(color= I('black'),fill=I('blue'),alpha = .5,shape =21)+
  geom_smooth(method = 'lm')+
  scale_x_continuous(limits = c(0,quantile(dm$carat,0.99)))+
  scale_y_continuous(limits = c(0,quantile(dm$price,0.99,na.rm = TRUE)))
```

***

### Frances Gerety
注释：de beers 公司的拥有者或者营销总监

#### 钻石是
经典广告词，a diamond is forever
***

### 钻石的兴起
注释：将钻石与婚姻的情感、愿望相关联，与男人的社会地位相关连，同时通过出借给上层阶层，明星名人，在重要的场合出现，比如奥斯卡颁奖典礼，热门电影中出现，将钻石与上流社会的相关连进行营销。

***

### ggpairs 函数
注释：

# 如有需要，安装下列组件
install.packages('GGally')
install.packages('scales')
install.packages('memisc')
install.packages('lattice')
install.packages('MASS')
install.packages('car')
install.packages('reshape')
install.packages('plyr')

# 加载 ggplot 图画组件及其他内容
library(ggplot2)
library(GGally)
library(scales)
library(memisc)

# 数据集中 10,000 个钻石的样本
```
```{r}
set.seed(20022012)
diamond_samp <- diamonds[sample(1:length(diamonds$price),10000),]
ggpairs(diamond_samp,
  lower = list(continuous = wrap("points", shape = I('.'))),
  upper = list(combo = wrap("box", outlier.shape = I('.'))),axisLabels = 'internal',cardinality_threshold = 15)
```

在 ggpairs 输出中你发现了什么？
回复：

***

### 钻石的需求
注释：

```{r The Demand of Diamonds}
p1 <- ggplot(aes(x = price),data = dm) +
  geom_histogram(binwidth =100 )+
  ggtitle('price distribution')+
  coord_cartesian(ylim = c(0,3000))
p2 <- ggplot(aes(x = price),data = dm) +
  geom_histogram(binwidth = 0.01)+
  ggtitle('price(log10) distribution')+
  scale_x_log10()
grid.arrange(p1,p2,ncol = 2)
  

```

***

### 创建一个新函数，改变变量 carat

```{r cuberoot transformation}
cuberoot_trans = function() trans_new('cuberoot', transform = function(x) x^(1/3),
                                      inverse = function(x) x^3)
```

#### 使用函数 cuberoot_trans
```{r Use cuberoot_trans}
ggplot(aes(carat, price), data = diamonds) + 
  geom_point() + 
  scale_x_continuous(trans = cuberoot_trans(), limits = c(0.2, 3),
                     breaks = c(0.2, 0.5, 1, 2, 3)) + 
  scale_y_continuous(trans = log10_trans(), limits = c(350, 15000),
                     breaks = c(350, 1000, 5000, 10000, 15000)) +
  ggtitle('Price (log10) by Cube-Root of Carat')
```

***

### 再次访问过度绘制

```{r Sort and Head Tables}
head(sort(table(dm$carat),decreasing = T))
head(sort(table(dm$price),decreasing = T))
```


```{r Overplotting Revisited}
ggplot(aes(carat, price), data = diamonds) + 
  geom_point(alpha =0.5,size = 3/4,position = 'jitter' )+
  scale_x_continuous(trans = cuberoot_trans(), limits = c(0.2, 3),
                     breaks = c(0.2, 0.5, 1, 2, 3)) + 
  scale_y_continuous(trans = log10_trans(), limits = c(350, 15000),
                     breaks = c(350, 1000, 5000, 10000, 15000)) +
  ggtitle('Price (log10) by Cube-Root of Carat')
```

### 价格 vs. 克拉与净度

改变下列代码。
```{r Price vs. Carat and Clarity}
# 安装并加载 RColorBrewer 组件
# install.packages('RColorBrewer')
# library(RColorBrewer)
```
```{r}
ggplot(aes(x = carat, y = price), data = diamonds) + 
  geom_point(alpha = 0.5, size = 1, position = 'jitter',aes(color=clarity)) +
  scale_color_brewer(type = 'div',
    guide = guide_legend(title = 'Clarity', reverse = T,
    override.aes = list(alpha = 1, size = 2))) +  
  scale_x_continuous(trans = cuberoot_trans(), limits = c(0.2, 3),
    breaks = c(0.2, 0.5, 1, 2, 3)) + 
  scale_y_continuous(trans = log10_trans(), limits = c(350, 15000),
    breaks = c(350, 1000, 5000, 10000, 15000)) +
  ggtitle('Price (log10) by Cube-Root of Carat and Clarity')
```
#### override.aes参数的作用？是guide_legend()函数的参数，主要是通过替换现有的参数值，比如alpha,size等等。
####guide_legend函数的作用?设置legend相关内容，比如title的位置和方向，label的位置和方向等等。
***

### 净度与价格
回复：

***

### 价格 vs. 克拉与切工

改变下列代码。
```{r Price vs. Carat and Cut}
ggplot(aes(x = carat, y = price, color = cut), data = diamonds) + 
  geom_point(alpha = 0.5, size = 1, position = 'jitter') +
  scale_color_brewer(type = 'div',
                     guide = guide_legend(title = 'Cut', reverse = T,
                                          override.aes = list(alpha = 1, size = 2))) +  
  scale_x_continuous(trans = cuberoot_trans(), limits = c(0.2, 3),
                     breaks = c(0.2, 0.5, 1, 2, 3)) + 
  scale_y_continuous(trans = log10_trans(), limits = c(350, 15000),
                     breaks = c(350, 1000, 5000, 10000, 15000)) +
  ggtitle('Price (log10) by Cube-Root of Carat and Cut')
```

***

### 切工与价格
回复：

***

### 价格 vs. 克拉与颜色

改变下列代码。
```{r Price vs. Carat and Color}
ggplot(aes(x = carat, y = price, color = color), data = diamonds) + 
  geom_point(alpha = 0.5, size = 1, position = 'jitter') +
  scale_color_brewer(type = 'div',
                     guide = guide_legend(title = 'Color', reverse = F,
                                          override.aes = list(alpha = 1, size = 2))) +  
  scale_x_continuous(trans = cuberoot_trans(), limits = c(0.2, 3),
                     breaks = c(0.2, 0.5, 1, 2, 3)) + 
  scale_y_continuous(trans = log10_trans(), limits = c(350, 15000),
                     breaks = c(350, 1000, 5000, 10000, 15000)) +
  ggtitle('Price (log10) by Cube-Root of Carat and Color')
```

***

### 颜色与价格
回复：

***

### R 中的线性模型
注释：

回复：

***

### 构建线性模型
Notes:

```{r Building the Linear Model}
m1 <- lm(I(log(price)) ~ I(carat^(1/3)), data = diamonds)
m2 <- update(m1, ~ . + carat)
m3 <- update(m2, ~ . + cut)
m4 <- update(m3, ~ . + color)
m5 <- update(m4, ~ . + clarity)
mtable(m1, m2, m3, m4, m5)
```

注意，把切工增加到我们模型中，为什么不利于解释钻石价格的方差。
之前没有探讨过这个问题。

***

### 模型中的问题
视频注释：

Research:
(花时间解决模型的 2-4 个问题)
(应该花费 10 到 20 分钟完成)

回复：

***

### 更大更优的数据集
Notes:

```{r A Bigger, Better Data Set}
install.package('bitops')
install.packages('RCurl')
library('bitops')
library('RCurl')

diamondsurl = getBinaryURL("https://raw.github.com/solomonm/diamonds-data/master/BigDiamonds.Rda")
load(rawConnection(diamondsurl))
```

这里可以获取得到数据所使用的代码：
https://github.com/solomonm/diamonds-data

## 使用更大的钻石数据集构建模型
Notes:

```{r Building a Model Using the Big Diamonds Data Set}

```

***

## 预测

BlueNile 钻石例子：
直径为 1.00 等级很好 I VS I 的钻石价格是 $5,601

```{r}
# 确保你已经加载了 memisc 库，保存为 m5 作为工作区的对象。
thisDiamond = data.frame(carat = 1.00, cut = "V.Good",
                         color = "I", clarity="VS1")
modelEstimate = predict(m5, newdata = thisDiamond,
                        interval="prediction", level = .95)
```

评估模型如何较好地预测 BlueNile 钻石价格。思考最佳点估计和 95% 的置信区间。

***

## 最终思考
注释：

***

点击 **KnitHTML** 查看你的成果和这节课的 html 页面、
答案和注释！
